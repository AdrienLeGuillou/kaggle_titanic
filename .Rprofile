#### -- Packrat Autoloader (version 0.4.8-1) -- ####
source("packrat/init.R")
#### -- End Packrat Autoloader -- ####

# if NVIM-R is present load external nvimcom
if (Sys.getenv("NVIMR_ID") != "") {
  packrat::extlib("nvimcom")
}
